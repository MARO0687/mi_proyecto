services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: apiGitHub
    image: api_github:latest
    ports:
      - "8080:8080"
    #restart: unless-stopped
    env_file:
      - .env
